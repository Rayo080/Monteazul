name: Wake up Supabase
on:
  schedule:
    - cron: '0 0 */3 * *' # Se ejecuta cada 3 días automáticamente
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Database
        run: curl ${{ secrets.SUPABASE_URL }}
